# MISC Group Schema Detail

# Generated by GSSDBAGENT on 2026-03-22 14:33 UTC



## MISC_FUNNEL (0 rows)



Tier: isolated

| # | Column | Type | Size | Nullable | Indexed | Key | Sample Values |

|---|--------|------|------|----------|---------|-----|---------------|

| 1 | SEQ | INTEGER | 4 | Yes | IDX #0 | [PK] | - |

| 2 | VALUE | CHAR | 50 | Yes | - |  | - |

### Indexes



| # | Type | Columns | Unique | Properties |

|---|------|---------|--------|------------|

| 0 | INDEX | SEQ | Yes |  |



## MISC_UF_DEFAULT (0 rows)



Tier: isolated

| # | Column | Type | Size | Nullable | Indexed | Key | Sample Values |

|---|--------|------|------|----------|---------|-----|---------------|

| 1 | MODE | INTEGER | 4 | Yes | - |  | - |

| 2 | IDTYPE | CHAR | 2 | Yes | IDX #0 | [PK] | - |

| 3 | UF_NO | INTEGER | 4 | Yes | IDX #0 | [PK] | - |

| 4 | UF_VALUE | CHAR | 30 | Yes | - |  | - |

| 5 | UFSID | INTEGER | 4 | Yes | IDX #0 | [PK] | - |

### Indexes



| # | Type | Columns | Unique | Properties |

|---|------|---------|--------|------------|

| 0 | INDEX | IDTYPE, UF_NO, UFSID | Yes |  |



## MISC_UF_VALUE (0 rows)



Tier: isolated

| # | Column | Type | Size | Nullable | Indexed | Key | Sample Values |

|---|--------|------|------|----------|---------|-----|---------------|

| 1 | CUST | CHAR | 7 | Yes | IDX #0 | [PK] | - |

| 2 | TYPE | CHAR | 2 | Yes | IDX #0 | [PK] | - |

| 3 | ID | CHAR | 9 | Yes | IDX #0 | [PK] | - |

| 4 | UF1 | CHAR | 30 | Yes | - |  | - |

| 5 | UF2 | CHAR | 30 | Yes | - |  | - |

| 6 | UF3 | CHAR | 30 | Yes | - |  | - |

| 7 | UF4 | CHAR | 30 | Yes | - |  | - |

| 8 | UF5 | CHAR | 30 | Yes | - |  | - |

| 9 | UF6 | CHAR | 30 | Yes | - |  | - |

| 10 | UF7 | CHAR | 30 | Yes | - |  | - |

| 11 | UF8 | CHAR | 30 | Yes | - |  | - |

| 12 | UF9 | CHAR | 30 | Yes | - |  | - |

| 13 | UF10 | CHAR | 30 | Yes | - |  | - |

| 14 | UF11 | CHAR | 30 | Yes | - |  | - |

| 15 | UF12 | CHAR | 30 | Yes | - |  | - |

| 16 | UF13 | CHAR | 30 | Yes | - |  | - |

| 17 | UF14 | CHAR | 30 | Yes | - |  | - |

| 18 | UF15 | CHAR | 30 | Yes | - |  | - |

| 19 | UF16 | CHAR | 30 | Yes | - |  | - |

### Indexes



| # | Type | Columns | Unique | Properties |

|---|------|---------|--------|------------|

| 0 | INDEX | CUST, TYPE, ID | Yes |  |



## MISC_UFTITLE (0 rows)



Tier: isolated

| # | Column | Type | Size | Nullable | Indexed | Key | Sample Values |

|---|--------|------|------|----------|---------|-----|---------------|

| 1 | MODE | INTEGER | 4 | Yes | - |  | - |

| 2 | IDTYPE | CHAR | 2 | Yes | IDX #0 | [PK] | - |

| 3 | UF_NO | INTEGER | 4 | Yes | IDX #0 | [PK] | - |

| 4 | UF_TITLE | CHAR | 30 | Yes | - |  | - |

### Indexes



| # | Type | Columns | Unique | Properties |

|---|------|---------|--------|------------|

| 0 | INDEX | IDTYPE, UF_NO | Yes |  |

## Relationships (Application-Layer FKs)

**AA_CONV_LOG** -> USER_INFORMATION (USER_MNL_CHG->USER_ID, USER_RUN->USER_ID)

**ACCTG_BRANCH** -> COMPANIES (CO_CODE -> COCODE); CUSTOMER_MASTER (CUSTNO -> CUSTOMER); GL_MASTER (AP_ACCOUNT->GL_ACCOUNT, AR_ACCOUNT->GL_ACCOUNT, INTERCO_AR->GL_ACCOUNT, INTERCO_GL->GL_ACCOUNT); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**ACH_DTL_INV** -> AP_OPEN_BATCHES (BATCH -> BATCH); AP_OPEN_ITEMS (INV_NO -> INVOICE); EXCHANGE_CURRENCY (CURR_SYMBOL->SYMBOL, EXCH_CURR->ISO_CODE); EXCHANGE_RATES (EXCH_DATE -> DATE_EXCH_RATE); GL_MASTER (CASH_ACCT -> GL_ACCOUNT); PO_HEADER (PURCH_ORD -> PURCHASE_ORDER); VENDOR_MASTER (VENNO -> VENDOR)

**ACH_PAYMENT** -> AP_OPEN_ITEMS (BATCH_NUM -> BATCH_NUM); USER_INFORMATION (LAST_CHG_BY -> GS_USER); VENDOR_MASTER (VENDOR_ID -> VENDOR)

**ALLOC_FOR_MIR** -> INVENTORY_MSTR (LOCN->LOCATION, PART->PART); ORDER_LINES (ORDER_NO->ORDER_NO, SEQ->RECORD_NO)

**ALT_COST_HIST** -> INVENTORY_MSTR (LOCN->LOCATION, PART->PART); USER_INFORMATION (HST_USER -> GS_USER)

**APPLY_USER_VAL** -> CUSTOMER_MASTER (CUST -> CUSTOMER); INVENTORY_MSTR (LOCN->LOCATION, PART->PART); OE_MULTI_SHIP (CUST->CUSTOMER, SHIPTO->SHIP_SEQ); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**APP_SETTING_HEADER** -> COMPANIES (COCODE -> COCODE); USER_GROUPS (GROUPID -> GROUP_ID); USER_INFORMATION (USERID -> USER_ID)

**APP_SETTING_LINES** -> APP_SETTING_HEADER (SETTINGID -> SETTINGID)

**AUTO_PURCHASING** -> EXCHANGE_CURRENCY (CURRENCY -> ISO_CODE); EXCHANGE_RATES (EXCH_DATE -> DATE_EXCH_RATE); INVENTORY_MSTR (LOCN->LOCATION, PART->PART); JOB_HEADER (WO->JOB, WO_SUFF->SUFFIX); JOB_OPERATIONS (WO_SEQ -> SEQ); ORDER_HEADER (SALES_ORDER -> ORDER_NO); ORDER_LINES (SO_LINE -> RECORD_NO); PL_MASTER (PROD_LINE -> PROD_LINE); PROJECT_MASTER (PROJECT -> PROJECT); PROJECT_PHASE (PHASE -> PHASE); UM_CODES (INV_UM->CODE, PO_UM->CODE); USER_INFORMATION (USERID -> GS_USER); VENDOR_MASTER (VENDOR->VENDOR, VENDOR_NAME->NAME_VENDOR)

**AUTO_WORK_ORDER** -> INVENTORY_MSTR (PART -> PART); ROUTER_HEADER (ROUTER -> ROUTER); UM_CODES (UM -> CODE)

**BACKLOG_HIST** -> CUSTOMER_MASTER (CUST_NO -> CUSTOMER)

**BALANCE_CHANGE** -> EMPLOYEE_MSTR (EMPLOYEE->EMPLOYEE, TIMATT_EMPLOYEE->EMPLOYEE); JOB_DETAIL (DTL_KEY_SEQ -> SEQUENCE_KEY); JOB_HEADER (DTL_JOB->JOB, DTL_SUFFIX->SUFFIX); JOB_OPERATIONS (DTL_SEQ -> SEQ)

**BIN_MASTER** -> LOCATION_ADDRESS (LOCATION -> LOCN)

**BIN_VAR** -> ITEM_MASTER (BIN->BIN, HEAT->HEAT, LOCN->LOCATION, LOT->LOT, PART->PART, SERIAL->SERIAL_NUMBER)

**BIR_ACTIVE_SEQ** -> BIR_GLOBAL (REPORT_ID -> REPORT_ID); BIR_GLOBAL_SEQ (REPORT_SEQUENCE -> REPORT_SEQUENCE); COMPANIES (COMPANY -> COCODE)

**BIR_DATA_SET_XREF** -> BIR_REPORT (BIR_REPORT_ID -> ID); REG_DATA_SOURCE_SET (DATA_SOURCE_SET_ID -> ID)

**BIR_DATA_SOURCE_XREF** -> BIR_REPORT (BIR_REPORT_ID -> ID); REG_DATA_SOURCE (DATA_SOURCE_ID -> ID)

**BIR_EMAIL_SEQ** -> BIR_GLOBAL (REPORT_ID -> REPORT_ID); BIR_GLOBAL_SEQ (REPORT_SEQUENCE -> REPORT_SEQUENCE); COMPANIES (COMPANY -> COCODE)

**BIR_GLOBAL** -> MENU_MODULE (MODULE -> MODULE_ID)

**BIR_GLOBAL_SEQ** -> BIR_GLOBAL (REPORT_ID -> REPORT_ID); BIR_REPORT (BIR_REPORT_ID->ID, GS_REPORT_ID->UUID)

**BIR_LOGO_SEQ** -> BIR_GLOBAL (REPORT_ID -> REPORT_ID); BIR_GLOBAL_SEQ (REPORT_SEQUENCE -> REPORT_SEQUENCE); COMPANIES (COMPANY -> COCODE)

**BIR_NOTES_SEQ** -> BIR_GLOBAL (REPORT_ID -> REPORT_ID); BIR_GLOBAL_SEQ (REPORT_SEQUENCE -> REPORT_SEQUENCE); COMPANIES (COMPANY -> COCODE)

**BIR_OVERRIDE_SEQ** -> BIR_GLOBAL (REPORT_ID -> REPORT_ID); BIR_GLOBAL_SEQ (REPORT_SEQUENCE -> REPORT_SEQUENCE); COMPANIES (COMPANY -> COCODE)

**BIR_PREPROC_GLOBAL** -> BIR_GLOBAL (REPORT_ID -> REPORT_ID); BIR_GLOBAL_SEQ (REPORT_SEQUENCE -> REPORT_SEQUENCE)

**BIR_PREPROC_USER** -> BIR_USER_SEQ (REPORT_ID->REPORT_ID, REPORT_SEQUENCE->REPORT_SEQUENCE); COMPANIES (COMPANY -> COCODE)

**BIR_PROC_LOG** -> BIR_GLOBAL (REPORT_ID -> REPORT_ID); BIR_GLOBAL_SEQ (REPORT_SEQUENCE -> REPORT_SEQUENCE); BIR_RUN (BIRUNID -> BIRUNID); COMPANIES (COMPANY_CODE -> COCODE); MENU_JOB_STREAM (JOB_STREAM_ID -> JOB_STREAM_ID); USER_INFORMATION (USER_ID -> USER_ID)

**BIR_REPORT_GROUP** -> BIR_REPORT (BIR_REPORT_ID -> ID); USER_GROUPS (GROUP_ID -> GROUP_ID)

**BIR_REPORT_USER** -> BIR_REPORT (BIR_REPORT_ID -> ID); USER_INFORMATION (USER_ID -> USER_ID)

**BIR_RUN** -> COMPANIES (COMPANY -> COCODE); MENU_JOB_STREAM (JOB_STREAM_ID -> JOB_STREAM_ID)

**BIR_RUN_COUNT** -> BIR_GLOBAL (RID -> REPORT_ID); BIR_GLOBAL_SEQ (SEQUENCE -> REPORT_SEQUENCE); COMPANIES (COCODE -> COCODE); USER_INFORMATION (LASTRUNUSERID -> USER_ID)

**BIR_SECURITY_SEQ** -> BIR_GLOBAL_SEQ (REPORT_ID->REPORT_ID, REPORT_SEQUENCE->REPORT_SEQUENCE); COMPANIES (COMPANY -> COCODE); USER_INFORMATION (SECURITY_ID -> USER_ID)

**BIR_USER** -> COMPANIES (COMPANY -> COCODE); MENU_MODULE (MODULE -> MODULE_ID)

**BIR_USER_SEQ** -> BIR_REPORT (BIR_REPORT_ID->ID, GS_REPORT_ID->UUID); BIR_USER (REPORT_ID -> REPORT_ID); COMPANIES (COMPANY -> COCODE)

**BP_MATL_H_NOTE** -> ORDER_HEADER (ORDER_NO -> ORDER_NO); ORDER_HIST_HEAD (INVOICE -> INVOICE); ORDER_LINES (ORDER_LINE -> RECORD_NO)

**BUYER_DESCRIPTOR** -> PO_MULTI_SHIP (SHIPTO -> SEQ)

**BUYING_GROUP** -> CUSTOMER_MASTER (CUSTOMER->CUSTOMER, GROUP_CUST->CUSTOMER); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**CARRIER_CODE_ADDR** -> AR_CUST_CARRIER (CARRIER_CD->CARRIER_CD, CUST_NO->CUST_NO, SHIPTO->SHIPTO, SOFTWARE->SOFTWARE, SRVC_TYPE->SRVC_TYPE); COUNTRY_CODES (COUNTRY_CD -> CODE)

**CHECKS_IN_USE** -> EXCHANGE_CURRENCY (EXCH_CURR -> ISO_CODE); GL_MASTER (GL_CASH_ACCT -> GL_ACCOUNT)

**CHECKS_LOG** -> GL_MASTER (ACCT_NO -> GL_ACCOUNT); VENDOR_MASTER (VEND_NO -> VENDOR)

**CHECK_VENDOR** -> EXCHANGE_CURRENCY (EXCH_CURR -> ISO_CODE); GL_MASTER (CASH_ACCT -> GL_ACCOUNT); VENDOR_MASTER (VENDOR -> VENDOR)

**CNTRCT_PRC_CMNT** -> CUSTOMER_MASTER (CUST -> CUSTOMER); EXCHANGE_CURRENCY (CURR -> ISO_CODE); INVENTORY_MSTR (PART -> PART)

**COMMISSIONS** -> CUSTOMER_MASTER (CUSTOMER -> CUSTOMER); ORDER_HEADER (ORDER_NO_KY -> ORDER_NO); ORDER_HIST_HEAD (INVOICE -> INVOICE); SALESPERSONS (SALESPERSON_KY -> ID)

**COMP_LANG** -> LANG_CODE (ISO_CODE -> ISO_CODE)

**CONSIGNMENT_TAX** -> GL_MASTER (ACCT -> GL_ACCOUNT); ORDER_LINES (LINE -> RECORD_NO); SHIPMENT_HEADER (SEQ -> ORDER_SUFFIX)

**CONTACT** -> CRM_CONTACT_AUX (ALT_ID -> CID); CUSTOMER_MASTER (CUST -> CUSTOMER); PROSPECT_MASTER (CUST -> PROSPECT); USER_INFORMATION (LAST_CHG_BY -> GS_USER); VENDOR_MASTER (CUST -> VENDOR)

**CONTACT_NOTES_AUX** -> USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**CONTAINER_RECVR** -> AR_TAX_TABLES (TAX_CODE->AUTH, TAX_CODE->ZONE); TAX_GROUP_RATE (TAX_CODE -> GRP_CD)

**CONTRACT_HEADER** -> CUSTOMER_MASTER (CUSTOMER -> CUSTOMER); EXCHANGE_CURRENCY (CURRENCY -> ISO_CODE); OE_MULTI_SHIP (SHIP_ID -> SHIP_SEQ)

**CONTRACT_LINES** -> CONTRACT_HEADER (CONTRACT->CONTRACT, CUSTOMER_PO->CUSTOMER_PO); CUSTOMER_MASTER (CUSTOMER -> CUSTOMER); EXCHANGE_CURRENCY (CURRENCY -> ISO_CODE); INVENTORY_MSTR (PART -> PART); LOCATION_ADDRESS (LOCATION -> LOCN); OE_MULTI_SHIP (SHIP_ID -> SHIP_SEQ); PART_XREF_USER (CUSTOMER_PART -> CUST_PART)

**COST_GOODS_SOLD** -> JOB_HEADER (SUFFIX->SUFFIX, WO->JOB); ORDER_HEADER (ORDER_NO -> ORDER_NO); ORDER_LINES (ORDER_LINE -> RECORD_NO)

**COST_POOLS** -> GL_MASTER (LABOR_GL_ACCT->GL_ACCOUNT, POOL_ID->COST_POOL)

**COURIER_EMAILATTACH** -> COURIER_EMAILBODY (EID -> EID)

**COURIER_EMAILBODY** -> COMPANIES (COCODE -> COCODE); USER_INFORMATION (USERID -> USER_ID)

**COURIER_EMAILHIST** -> COMPANIES (COCODE -> COCODE); USER_INFORMATION (USERID -> USER_ID)

**COURIER_EMLHISTATT** -> COURIER_EMAILHIST (EHID -> EHID)

**COURIER_EMLHISTBODY** -> COURIER_EMAILHIST (EHID -> EHID)

**CUTLIST_BASIS** -> CUTLIST_BASIS (CUT_DATE->CUT_TIME, CUT_TIME->CUT_DATE); JOB_CT_FLX_SCHD (SCHEDULE->SCHEDULE, SCHED_SUFFIX->SCHEDULE_SUFFIX); JOB_HEADER (WO->JOB, WO_SUFFIX->SUFFIX); JOB_OPERATIONS (WO_SEQ -> SEQ)

**CYCLE_COUNT_BIN** -> INVENTORY_MSTR (PART -> PART); ITEM_MASTER (BIN->BIN, HEAT->HEAT, LOT->LOT, SERIAL->SERIAL_NUMBER); PL_MASTER (PL -> PROD_LINE); UM_CODES (UM -> CODE)

**CYCLE_COUNT_XREF** -> CYCLE_COUNT_BIN (RUN_ID -> RUNID)

**DAISY_OPT_BOM** -> INVENTORY_MSTR (ITEM -> PART)

**DAISY_OPT_XREF** -> BOM_MSTR (MODEL_BOM->PARENT, OPTION_BOM->PARENT)

**DASHBOARD_GROUP** -> DASH_MASTER (DASHBOARD_ID -> ID); USER_GROUPS (GROUP_ID -> GROUP_ID)

**DASHBOARD_USER** -> DASH_MASTER (DASHBOARD_ID -> ID); USER_INFORMATION (USER_ID -> USER_ID)

**DASH_AUTOSAVE** -> DASH_MASTER (DASHBOARD_UUID -> UUID); USER_INFORMATION (USER_ID -> USER_ID)

**DASH_DATA_XREF** -> DASH_MASTER (DASHBOARD_ID -> ID); REG_DATA_SOURCE (DATA_SOURCE_SET_ID -> ID); REG_DATA_SOURCE_SET (DATA_SOURCE_ID -> ID)

**DASH_MASTER** -> DASH_SET (SET_ID -> ID)

**DASH_META** -> DASH_MASTER (DASHBOARD_UUID -> UUID); DASH_VERSIONS (VERSION_ID -> ID)

**DASH_VERSIONS** -> DASH_MASTER (DASHBOARD_UUID -> UUID); USER_INFORMATION (USER_ID -> USER_ID)

**DATA_SOURCE_GROUP** -> REG_DATA_SOURCE (DATA_SOURCE_ID -> ID); USER_GROUPS (GROUP_ID -> GROUP_ID)

**DATA_SOURCE_USER** -> REG_DATA_SOURCE (DATA_SOURCE_ID -> ID); USER_INFORMATION (USER_ID -> USER_ID)

**DELETED_INV_HLD** -> DELETED_INV_HLD (F_DATE->F_TIME, F_TIME->F_DATE); ORDER_HEADER (ORDER_NO -> ORDER_NO); ORDER_HIST_HEAD (INVOICE -> INVOICE); SHIPMENT_HEADER (SEQ -> ORDER_SUFFIX)

**DEPOSIT_IDS** -> GL_MASTER (GL_ACCT -> GL_ACCOUNT); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**DEVICE_AUX_INFO** -> DEVICE_COMPUTER (DID -> DID)

**DEVICE_COMPONENTS** -> DEVICE_COMPUTER (DCID -> DCID)

**DEVICE_IP** -> DEVICE_COMPUTER (ID -> DCID)

**DEVICE_NOTES** -> DEVICE_COMPUTER (DID -> DID)

**DEVICE_TYPE** -> DEVICE_COMPUTER (DID -> DID)

**DOCK_TYPE** -> USER_INFORMATION (MODIFIED_BY -> USER_ID)

**END_CUST_XREF** -> CUSTOMER_MASTER (DISTRIBUTOR -> CUSTOMER); END_CUSTOMER (END_CUST -> END_CUST)

**EQUIP** -> CUSTOMER_MASTER (CUST -> CUSTOMER); OE_MULTI_SHIP (SHIP_TO -> SHIP_SEQ)

**EQUIP_NOTES** -> EQUIP (EQUIP -> EQUIP_NO)

**EXCHANGE_RATES** -> EXCHANGE_CURRENCY (FROM_ISO_CODE->ISO_CODE, TO_ISO_CODE->ISO_CODE)

**FACILITY_CODES** -> USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**FC_DTL_MASTER** -> FORECAST_SUMMARY (ID -> ID)

**FG_STD_COST** -> INVENTORY_MSTR (LOCATION->LOCATION, PART->PART)

**FIXED_ASSETS** -> GL_MASTER (GL_BALANCE->GL_ACCOUNT, GL_DEP->GL_ACCOUNT, GL_RES->GL_ACCOUNT); ITEM_MASTER (SERIAL -> SERIAL_NUMBER); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**FLEX_SCHD_BASIS** -> EMPLOYEE_MSTR (EMPNUM -> EMPLOYEE); INVENTORY_MSTR (LOC->LOCATION, PART_NO->PART); JOB_CT_FLX_SCHD (SCHEDULE->SCHEDULE, SCHED_SUF->SCHEDULE_SUFFIX)

**FLEX_SCHD_BASIS_D** -> INVENTORY_MSTR (LOC->LOCATION, PART_NO->PART); JOB_CT_FLX_SCHD (SCHEDULE->SCHEDULE, SCHED_SUF->SCHEDULE_SUFFIX); JOB_HEADER (WO->JOB, WO_SUF->SUFFIX)

**FUNCTION_SECURITY** -> COMPANIES (COCODE -> COCODE); FUNCTION_SEC_ITEMS (FUNCTIONID -> FUNCTIONID); USER_GROUPS (GROUPID -> GROUP_ID); USER_INFORMATION (LASTCHANGEDBY->USER_ID, USERID->USER_ID)

**FUNCTION_SEC_GROUP** -> INTL_LABEL (INTLLABELID -> ID)

**FUNCTION_SEC_ITEMS** -> FUNCTION_SEC_GROUP (FUNCTIONGROUPID -> FUNCTIONGROUPID); INTL_LABEL (INTLLABELID -> ID)

**GS_REGISTRY** -> COMPANIES (COMPANY -> COCODE); USER_INFORMATION (USER_ID -> USER_ID)

**HF_LOG** -> HF_QUEUE (INSTALLID -> INSTALLID); USER_INFORMATION (USER_ID -> USER_ID)

**HF_QUEUE** -> USER_INFORMATION (USERID -> USER_ID)

**HF_QUEUEDETAIL** -> HF_QUEUE (INSTALLID -> INSTALLID)

**HF_QUEUELINES** -> HF_QUEUE (INSTALLID -> INSTALLID)

**HIST_PO_LINE** -> PO_HEADER (PO -> PURCHASE_ORDER); PO_LINES (PO_LINE -> RECORD_NO)

**IND_LBR_ACCTS** -> GL_MASTER (DIRECT_GL_ACCT->GL_ACCOUNT, INDIRECT_GL_ACCT->GL_ACCOUNT); JOB_HEADER (JOB->JOB, SUFFIX->SUFFIX); JOB_OPERATIONS (SEQUENCE -> SEQ)

**INTCMP_SHP_EVNT** -> ORDER_HEADER (SHIP_ORDER -> ORDER_NO); ORDER_LINES (SHIP_LINE -> RECORD_NO); PO_LINES (PO_LINE -> RECORD_NO); SHIPMENT_LINES (ASCT_LINE->ORDER_REC, ASCT_ORDER->ORDER_NO, ASCT_SEQ->ORDER_SUFFIX); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**INTCMP_SO_EVNT** -> EXCHANGE_CURRENCY (CURR_CD -> ISO_CODE); INVENTORY_MSTR (PART -> PART); LOCATION_ADDRESS (LOCN -> LOCN); ORDER_LINES (DEST_ORDER_LN->RECORD_NO, DEST_ORDER_NO->ORDER_NO, ORDER_LINE->RECORD_NO, ORDER_NO->ORDER_NO); PO_HEADER (PO_NO -> PURCHASE_ORDER); PO_LINES (PO_LINE -> RECORD_NO); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**INTERCMPNY_XREF** -> COMPANIES (DEST_CMPNY->COCODE, ORGN_CMPNY->COCODE); CUSTOMER_MASTER (DEST_CUST_NO -> CUSTOMER); USER_INFORMATION (LAST_CHG_BY -> GS_USER); VENDOR_MASTER (ORGN_VENDOR -> VENDOR)

**INTL_CUSTOM** -> INTL_LANGUAGE_CODE (LANGUAGE_CODE -> LANGUAGE_CODE)

**INTL_LABEL** -> INTL_LANGUAGE_CODE (LANG_CD -> LANGUAGE_CODE); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**INTL_MSG_TXT** -> USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**JE_OPEN_BATCHES** -> DEPARTMENTS (DEPT -> DEPT_ID); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**LABOR_MATERIAL** -> WORKCENTERS (REC_KEY -> MACHINE)

**LANG_CODE** -> LANG_CODE (ISO_CODE -> ISO_CODE)

**LEARNING_CURVE** -> OP_CODES (OPCODE -> OPERATION); WORKCENTERS (WC -> MACHINE)

**LIFO_FIFO** -> INVENTORY_MSTR (PART -> PART); LOCATION_ADDRESS (LOCATION -> LOCN)

**LOAD_PLAN** -> BIN_MASTER (BIN_LOC->LOCATION, STAGING_BIN->BIN); CUSTOMER_MASTER (CUSTOMER -> CUSTOMER); DOCK_TYPE (DOCK_NO -> DOCK_NO); MOBILE_USER_INFO (ASSIGNED_TO -> USERID); ORDER_LINES (ORDER_LINE->RECORD_NO, ORDER_NO->ORDER_NO); SHIPMENT_LINES (ORDER_SUFFIX -> ORDER_SUFFIX); TRUCK_TYPE (TRUCK_TYPE -> TYPE_ID); USER_INFORMATION (MODIFIED_BY -> USER_ID)

**LOAD_PLAN_ITEMS** -> INVENTORY_MSTR (LOC->LOCATION, PART->PART); ITEM_MASTER (BIN->BIN, HEAT->HEAT, LOT->LOT, SERIAL_NUMBER->SERIAL_NUMBER); ORDER_LINES (ORDER_LINE->RECORD_NO, ORDER_NO->ORDER_NO); SHIPMENT_LINES (ORDER_SUFFIX -> ORDER_SUFFIX); USER_INFORMATION (MODIFIED_BY_USER -> USER_ID)

**LOCATION_ADDRESS** -> CUSTOMER_MASTER (TRNF_CUST -> CUSTOMER); CUST_FORM_INFO (LOCN -> DEFAULT_LOC); GL_MASTER (ACCRUAL_ACCT->GL_ACCOUNT, BOOK_VAR_ACCT->GL_ACCOUNT, FRT_ACCT->GL_ACCOUNT, FRT_SALES_ACCT->GL_ACCOUNT); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**LOCATION_PRIORITY** -> LOCATION_ADDRESS (LOCN->LOCN, ORGN_LOCN->LOCN); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**LUNCH_SHIFT** -> BALANCING_GROUP (SHIFT_GROUP -> BAL_GROUP); DEPARTMENTS (DEPT -> DEPT_ID); JOB_HEADER (BAL_JOB->JOB, BAL_SUFFIX->SUFFIX, BRK_JOB->JOB, BRK_SUFFIX->SUFFIX, LUN_JOB->JOB, LUN_SUFFIX->SUFFIX); JOB_OPERATIONS (BAL_SEQ->SEQ, BRK_SEQ->SEQ, LUN_SEQ->SEQ); PAYROLL_PAY_CATS (EC -> PAY_CATEGORY); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**MATERIAL_LOCATOR** -> INVENTORY_MSTR (PART -> PART); ITEM_MASTER (SERIAL -> SERIAL_NUMBER)

**MATERIAL_REQ_PLAN** -> INVENTORY_MSTR (PART -> PART)

**MFGCFG_INQT_NOTES** -> CFG_OPT_NOTES (ID -> ID); QUOTE_HEADER (QUOTE_NO -> QUOTE_NO); QUOTE_LINES (LINE -> RECORD_NO)

**MFG_CONFIG_NOTES** -> CONFIG_MFG (BASEID->BASE_ID, ID->ID_NUM, SEQ->CFG_SEQUENCE)

**MFG_INS_ORD_NOTES** -> CONFIG_ORD_INSERT (BASEID->BASEID, ID->ID, ORDER_LINE->ORDER_LINE, ORDER_NO->ORDER_NO, SEQ->SEQ)

**MIR_ABSENTEE_INFO** -> DEPARTMENTS (DEPT -> DEPT_ID)

**MOBILE_CUSTOM_LABELS** -> MOBILE_CUSTOM_TRANS (TXID -> TXID)

**MOBILE_CUSTOM_OPTION** -> MOBILE_CUSTOM_LABELS (LID -> LID); MOBILE_CUSTOM_TRANS (TXID -> TXID)

**MOBILE_CUSTOM_RESULT** -> COMPANIES (COCODE -> COCODE); MOBILE_CUSTOM_TRANS (TXID -> TXID)

**MOBILE_DEVICE_ATTRIB** -> MOBILE_DEVICE_GROUP (GID -> GID)

**MOBILE_SHIPPING_INFO** -> SHIPMENT_HEADER (PCK_NO -> PCK_NO)

**MOBILE_USER_COMPANY** -> COMPANIES (COCODE -> COCODE); MOBILE_USER_INFO (UID -> UID)

**MOBILE_USER_INFO** -> EMPLOYEE_MSTR (EMPLOYEE_ID -> EMPLOYEE); USER_INFORMATION (GSUSERNAME->GS_USER, USERID->USER_ID)

**MOBILE_USER_PRINTER** -> MOBILE_USER_INFO (UID -> UID)

**MOBILE_USER_XACT** -> MOBILE_CUSTOM_TRANS (TXID -> TXID); MOBILE_USER_INFO (UID -> UID)

**MSG_CONDITIONS** -> MSG_CONFIG (CONFIGURATION_ID -> CONFIGURATION_ID); MSG_EVENT_PARM_VALS (VALUE_PARAMETER_ID -> VALUE_ID); MSG_PARAMETERS (PARAMETER_ID -> PARAMETER_ID)

**MSG_CONFIG** -> COMPANIES (COMPANY_CODE -> COCODE); INTL_LABEL (NAME_ID -> ID); MSG_EVENT (EVENT_ID -> EVENT_ID)

**MSG_EVENT** -> INTL_LABEL (NAME->LABEL, NAME_ID->ID)

**MSG_EVENT_PARAMETERS** -> MSG_EVENT (EVENT_ID -> EVENT_ID)

**MSG_EVENT_PARM_VALS** -> INTL_LABEL (DISPLAY_VALUE_ID -> ID); MSG_EVENT_PARAMETERS (EVENT_PARAMETER_ID -> EVENT_PARAMETER_ID)

**MSG_MESSAGES** -> MSG_CONFIG (CONFIGURATION_ID -> CONFIGURATION_ID); USER_INFORMATION (TRIGGERED_BY -> USER_ID)

**MSG_PARAMETERS** -> INTL_LABEL (DISPLAY_NAME_ID -> ID)

**MSG_QUEUE** -> MSG_RECIPIENTS (RECIPIENT -> MESSAGE_RECIPIENT_ID)

**MSG_RECIPIENTS** -> INTL_LANGUAGE_CODE (LANGUAGE_CODE_1->LANGUAGE_CODE, LANGUAGE_CODE_2->LANGUAGE_CODE); MSG_CONFIG (CONFIGURATION_ID -> CONFIGURATION_ID); USER_INFORMATION (RECIPIENT -> USER_ID)

**OEGRP_PRICE_CMMNT** -> OE_GRP_PRICING (CD->CD, CURR_CD->CURR_CD, KEY_DATE->KEY_DATE, LOCN->LOCN, PART->PART)

**ONLINE_ERR_LOG** -> EMPLOYEE_MSTR (EMPLOYEE -> EMPLOYEE); INVENTORY_MSTR (PART -> PART); JOB_HEADER (JOB->JOB, SUFFIX->SUFFIX); JOB_OPERATIONS (SEQUENCE -> SEQ); LOCATION_ADDRESS (LOCN -> LOCN); WORKCENTERS (WORKCENTER -> MACHINE)

**ONTIME** -> CUSTOMER_MASTER (CUSTOMER -> CUSTOMER)

**OPCODE_SUPP_FORM** -> OP_CODES (LMO -> LMO)

**OPCODE_TEXT** -> OP_CODES (LMO -> LMO)

**OPEN_QUOTES** -> INVENTORY_MSTR (PART_LOCN->LOCATION, PART_NO->PART)

**OP_CODES** -> OPCODE_TEXT (LMO -> LMO); PROJECT_GROUP (PROJ_GROUP -> F_GROUP); UM_CODES (UNIT_MEASURE -> CODE)

**OP_HEADER** -> USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**ORGN_TAX_ADDR** -> ORGN_TAX_ADR_CODE (ORGN_ADR -> ORGN_ADR); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**ORGN_TAX_ADR_CODE** -> AR_TAX_TABLES (TAX_AUTH->AUTH, TAX_ZONE->ZONE)

**PAYLOCK** -> COMPANIES (COMPANY -> COCODE); USER_INFORMATION (USER_ID -> USER_ID)

**PAYMENT** -> CUSTOMER_MASTER (CUST_NO -> CUSTOMER); OE_MULTI_SHIP (SHIPTO -> SHIP_SEQ); ORDER_HEADER (ORDER_NO -> ORDER_NO); USER_INFORMATION (USER_ID -> GS_USER)

**PAYMENT_2** -> CUSTOMER_MASTER (CUST_NO -> CUSTOMER); ORDER_HEADER (SHIPMENT_ORDER -> ORDER_NO); SHIPMENT_HEADER (SHIPMENT_SEQ -> ORDER_SUFFIX); USER_INFORMATION (USER_ID -> GS_USER)

**PAYMENT_3** -> CUSTOMER_MASTER (CUST_NO -> CUSTOMER); ORDER_HEADER (CC_ORDER_NO -> ORDER_NO); ORDER_HIST_HEAD (CC_INVOICE_NO -> INVOICE); SHIPMENT_HEADER (CC_SEQ_NO -> ORDER_SUFFIX); USER_INFORMATION (USER_ID -> GS_USER)

**PLASTICS_CALC** -> INVENTORY_MSTR (PART -> PART); ROUTER_HEADER (ROUTER -> ROUTER); ROUTER_LINE (SEQ -> LINE_ROUTER); UM_CODES (UM -> CODE)

**PRICECLASS_PL_DISC** -> OE_PRC_CLASS_DISC (PRICE_CLASS -> PRC_CODE); PL_MASTER (PROD_LINE -> PROD_LINE)

**PURCHASE_REQS** -> AR_TAX_TABLES (TAX_CODE->AUTH, TAX_CODE->ZONE); BUYER_DESCRIPTOR (BUYER -> BUYER_CODE); COMPANY_CURRENCY (EXCH_CURR -> ISO_CODE); GL_MASTER (GL_ACCOUNT -> GL_ACCOUNT); INVENTORY_MSTR (PART -> PART); INV_CROSS_REF (MFG_PART_NO -> MFG_PART); JOB_HEADER (JOB->JOB, SUFFIX->SUFFIX); JOB_OPERATIONS (SEQUENCE -> SEQ); LOCATION_ADDRESS (LOCATION -> LOCN); PL_MASTER (PRODUCT_LINE -> PROD_LINE); PO_HEADER (PO -> PURCHASE_ORDER); PO_LINES (PO_LINE -> RECORD_NO); REQSN_TEXT (PO_REQ_LINE->LINE, PO_REQ_NUMBER->NUMBER); SALESPERSONS (SALESPERSON -> ID); TAX_GROUP_RATE (TAX_CODE -> GRP_CD); UM_CODES (INVENTORY_UM->CODE, UM->CODE); VAT_TAX_RULE (VAT_RULE -> RULE_ID); VENDOR_MASTER (VENDOR -> VENDOR)

**PUR_MRP_1** -> INVENTORY_MSTR (PART -> PART); JOB_HEADER (WO->JOB, WO_SUFFIX->SUFFIX); JOB_OPERATIONS (WO_SEQ -> SEQ); LOCATION_ADDRESS (PART_LOC -> LOCN); PO_HEADER (PO -> PURCHASE_ORDER); PO_LINES (PO_LINE -> RECORD_NO); VENDOR_MASTER (VENDOR -> VENDOR)

**PUR_MRP_2** -> INVENTORY_MSTR (PART -> PART); LOCATION_ADDRESS (PART_LOC -> LOCN); PO_HEADER (BLANKET_PO -> PURCHASE_ORDER); PO_LINES (PO_LINE -> RECORD_NO); VENDOR_MASTER (VENDOR -> VENDOR)

**P_ADDER_PRICING** -> P_ADDER_QTE (METAL -> METAL); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**P_ADDER_QTE** -> INVENTORY_MSTR (PART -> PART); QUOTE_HEADER (QUOTE_NBR -> QUOTE_NO); UM_CODES (UM -> CODE)

**QT_CONFIG_NOTES** -> QUOTE_CONFIG (LINE->QUOTE_LINE, QUOTE_NO->QUOTE_NO)

**RATE_CHANGE** -> INVENTORY_MSTR (PART -> PART); RATE_CHANGE (CHG_DATE->CHG_TIME, CHG_TIME->CHG_DATE)

**RDR_REPORT_GRID_MODE** -> RDR_REPORT_GRID (ID_REPORT_GRID -> ID_REPORT_GRID); USER_INFORMATION (USER_ID -> USER_ID)

**REFUND_XREF** -> CUSTOMER_MASTER (CUST -> CUSTOMER); GL_MASTER (GL_ACCT -> GL_ACCOUNT); ORDER_HIST_HEAD (INVC -> INVOICE)

**REG_DATA_SOURCE** -> COMPANIES (LINKED_COMPANY -> COCODE); REG_DATA_SOURCE_SET (SET_ID -> ID)

**REG_DATA_SOURCE_CRED** -> REG_DATA_SOURCE (DATA_SOURCE_ID -> ID)

**REG_QUERY** -> REG_QUERY_SET (SET_ID -> ID)

**REQ_APPRV_HIS** -> PURCHASE_REQS (NUM -> PO_REQ_NUMBER)

**REQ_HIST** -> AR_TAX_TABLES (TAX_CODE->AUTH, TAX_CODE->ZONE); EXCHANGE_CURRENCY (EXCH_CURR -> ISO_CODE); EXCHANGE_RATES (EXCH_DATE->DATE_EXCH_RATE, EXCH_RATE->EXCHANGE_RATE); GL_MASTER (GL_ACCT -> GL_ACCOUNT); INVENTORY_MSTR (LOCN->LOCATION, PART->PART); JOB_HEADER (SUFF->SUFFIX, WO->SUFFIX); JOB_OPERATIONS (SEQ -> SEQ); PO_HEADER (PO -> PURCHASE_ORDER); PO_LINES (PO_LINE -> RECORD_NO); REQSN_H_TEXT (REQ_LINE->LINE, REQ_NUM->NUMBER); SALESPERSONS (SALESMAN -> ID); TAX_GROUP_RATE (TAX_CODE -> GRP_CD); UM_CODES (INV_UM->CODE, UM->CODE); VENDOR_MASTER (VENDOR -> VENDOR)

**RETURNS** -> ACCTG_BRANCH (BRANCH -> BRANCH_ID); AP_SHIP_FOB (FOB_INFO -> CODE); AP_TERMS_INFO (TERMS -> CODE); AP_VIA_SHIP (SHIP_VIA -> CODE); BUYER_DESCRIPTOR (BUYER -> BUYER_CODE); COMPANY_CURRENCY (EXCH_CURR -> ISO_CODE); DEPARTMENTS (DEPT_A -> DEPT_ID); GL_MASTER (GL_ACCOUNT->GL_ACCOUNT, NORMAL_GL_ACCT->GL_ACCOUNT); INVENTORY_MSTR (PART -> PART); JOB_HEADER (JOB->JOB, JOB_SUFF->SUFFIX); JOB_OPERATIONS (JOB_SEQ -> SEQ); LOCATION_ADDRESS (LOCN -> LOCN); PL_MASTER (PROD_LINE -> PROD_LINE); PO_HEADER (PO -> PURCHASE_ORDER); PO_LINES (PO_LINE -> RECORD_NO); PO_RECEIVER (RECEIVER -> RECEIVER_NO); PURCHASE_REQS (REQ_NO -> PO_REQ_NUMBER); UM_CODES (ALT_UM->CODE, UM->CODE); VENDOR_MASTER (VENDOR -> VENDOR)

**RTGBOM_XREF** -> BOM_MSTR (BOM_ID -> ID); ROUTER_SUPP_LINE (ROUTER_ID -> ID); RTGBOM_META (METADATA_ID -> ID)

**SALESPERSONS** -> USER_INFORMATION (LAST_USER_CHG -> GS_USER)

**SALESTEAM_MEMBERSHIP** -> SALESPERSONS (SALES_PERSON_ID -> ID); SALES_TEAM (ID_SALES_TEAM -> ID_SALES_TEAM)

**SCRAP_DATA** -> JOB_DETAIL (DTL_DATE->DATE_SEQUENCE, DTL_KEY_SEQ->SEQUENCE_KEY); JOB_HEADER (DTL_JOB->JOB, DTL_SUFFIX->SUFFIX); JOB_OPERATIONS (DTL_CAT -> SEQ); QUALITY (QUALITY -> CONTROL_NUMBER); REJECTED_CODE (REASON_CODE -> CODE)

**SCREW_MACH_SCRAP** -> INVENTORY_MSTR (MATERIAL -> PART); ROUTER_HEADER (ROUTER_NUM -> ROUTER)

**SECUSER_PAY** -> PAYROLL_PAY (PAY_GROUP -> PAY_GROUP)

**SERIAL_ISSUE** -> BIN_MASTER (BIN -> BIN); INVENTORY_MSTR (PART -> PART); ITEM_MASTER (HEAT->HEAT, LOT->LOT, SERIAL->SERIAL_NUMBER); JOB_HEADER (JOB->JOB, SUFF->SUFFIX); JOB_OPERATIONS (SEQUENCE -> SEQ); LOCATION_ADDRESS (LOCN -> LOCN)

**SHIFT_CAL_YR** -> BALANCING_GROUP (SHIFT_GROUP -> BAL_GROUP); DEPARTMENTS (DEPT -> DEPT_ID); LUNCH_SHIFT (SHIFT -> SHIFT)

**SIMPLE_ACCT_GRP** -> GL_MASTER (INV_CR_ACCT->GL_ACCOUNT, INV_DB_ACCT->GL_ACCOUNT, WIP_LABOR_CR_ACCT->GL_ACCOUNT, WIP_LABOR_DB_ACCT->GL_ACCOUNT, WIP_MATL_CR_ACCT->GL_ACCOUNT, WIP_MATL_DB_ACCT->GL_ACCOUNT, WIP_OH_CR_ACCT->GL_ACCOUNT, WIP_OH_DB_ACCT->GL_ACCOUNT)

**SPT_TICKET_FILES** -> SUPPORT_TICKETS (STID -> STID)

**STATE_CODE_TAX** -> GL_MASTER (TAX_LIAB_ACCT->GL_ACCOUNT, VAT_ACCR_ACCT->GL_ACCOUNT, VAT_EXP_ACCT->GL_ACCOUNT)

**SUPPLMNTL_SCHED** -> JOB_OPERATIONS (SEQ->SEQ, WO->JOB, WO_SUFFIX->SUFFIX)

**SUPPORT_TICKETS** -> COMPANIES (COCODE -> COCODE); INT_SUPPORT_STATUS (STATUS -> ISSID); INT_SUPPORT_TYPE (TYPE -> ISTID); USER_INFORMATION (USERID -> USER_ID)

**SURVEY_XREF** -> ORDER_HEADER (ORDER_NO -> ORDER_NO); ORDER_LINES (ORDER_LN -> RECORD_NO)

**TASKS** -> USER_INFORMATION (USERID -> USER_ID)

**TIMAT_NOTES** -> TIME_ATTENDANCE (CHARGE_DATE->CHARGE_DATE, EMPLOYEE->EMPLOYEE, IN_TS_MIN->IN_TS_MIN)

**TRUCK_TYPE** -> USER_INFORMATION (MODIFIED_BY -> USER_ID)

**UNATTENDED_TASK_LOG** -> COMPANIES (COMPANY -> COCODE)

**USR_MENU_GRPS** -> USER_INFORMATION (USR_ID -> USER_ID)

**VALUE_ADDED** -> ROUTER_HEADER (ROUTER -> ROUTER); ROUTER_LINE (SEQUENCE -> LINE_ROUTER)

**VAR_BUDGET** -> GL_MASTER (ACCOUNT->GL_ACCOUNT, FROM_ACCT->GL_ACCOUNT, TO_ACCT->GL_ACCOUNT); USER_INFORMATION (LAST_CHG_BY -> GS_USER)

**WARRANTY** -> CUSTOMER_MASTER (CUSTOMER->CUSTOMER, PURCH_FROM_ID->CUSTOMER); INVENTORY_MSTR (PART -> PART); ITEM_MASTER (OLD_SERIAL->SERIAL_NUMBER, SERIAL->SERIAL_NUMBER); LOCATION_ADDRESS (LOCATION -> LOCN); ORDER_HEADER (ORDER_NO -> ORDER_NO); ORDER_HIST_HEAD (INVOICE -> INVOICE); ORDER_LINES (ORDER_LINE -> RECORD_NO); SHIPMENT_HEADER (ORDER_SUFFIX -> ORDER_SUFFIX)

**WARRANTY_COMMENTS** -> INVENTORY_MSTR (PART -> PART); ITEM_MASTER (SERIAL -> SERIAL_NUMBER); LOCATION_ADDRESS (LOCN -> LOCN)

**WARRANTY_NOTES** -> INVENTORY_MSTR (PART -> PART); ITEM_MASTER (SERIAL -> SERIAL_NUMBER)

**WL_AUDIT_LOG** -> WIRELESS_HDR (TRANS_ID -> TRANS_ID); WIRELESS_LINE (LINE_SEQ -> SEQ)

**WORKCENTERS** -> COST_POOLS (COST_POOL_CODE -> POOL_ID); DEPARTMENTS (WC_DEPT -> DEPT_ID); PAYROLL_PAY_CATS (EARNINGS_CODE -> PAY_CATEGORY); PROJECT_GROUP (PROJ_GROUP -> F_GROUP)

**WORKGROUP_HEAD** -> WORKCENTERS (PROTOTYPE_WC -> MACHINE)

**WORKGROUP_LINE** -> WORKCENTERS (WORKCENTER -> MACHINE); WORKGROUP_HEAD (WORKGROUP -> WORK_GROUP)

**WORKLOAD_HEADER** -> COMPANIES (COCODE -> COCODE)

**WORKLOAD_PARAM** -> WORKLOAD_HEADER (WID -> WID)

**X_LOT_BIN** -> BIN_MASTER (BIN -> BIN); ITEM_MASTER (SERIAL -> SERIAL_NUMBER); JOB_DETAIL (KEY_SEQ -> SEQUENCE_KEY); JOB_HEADER (JOB->JOB, SUFFIX->SUFFIX); JOB_OPERATIONS (SEQ -> SEQ)

